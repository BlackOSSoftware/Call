import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.quickdialer.app",
  appName: "Quick Dialer",
  webDir: "out",
  android: {
    backgroundColor: "#0A0A0A",
  },
};

export default config;
